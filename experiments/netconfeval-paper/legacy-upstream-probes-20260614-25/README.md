# Legacy upstream probe results

These CSV files were recovered from the retired local FYP checkout during the
2026-07 workspace consolidation.

- They capture exploratory DeepSeek runs from 2026-06-14 and 2026-06-25.
- The maintained reproduction results elsewhere in `experiments/` remain the
  canonical paper-reproduction artifacts.
- Empty outputs, local paths, virtual environments, upstream source snapshots,
  and raw logs were intentionally excluded.
- The old direct edit to upstream `model_configs.py` was not copied because
  `docker/netconfeval-paper/scripts/patch_deepseek_model.py` already provides the
  maintained compatibility path.
