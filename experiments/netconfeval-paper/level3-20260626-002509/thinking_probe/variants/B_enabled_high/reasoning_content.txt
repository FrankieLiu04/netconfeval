We are to generate a Python function `compute_routing_paths()` that takes network topology (as described) and network requirements (default None) and returns routing paths as described.

We need to output JSON with status "OK" and result being the function code as a string.

The function should:

- Parse the topology: Dict[str, Dict[int, str]].
- Build a graph where nodes are devices (hosts and switches) and edges are LANs. Actually, we need to connect devices that share the same LAN.

We can treat each LAN as a node? Or we can find connections: For each device, for each port, the LAN identifier. Devices that share a LAN are directly connected. So we can create an adjacency list: for each LAN, collect all devices connected to it. Then for each such set, add edges between all pairs? But that would be complete graph. However, the typical network is a switch connecting to hosts via LANs. But the given topology might have multiple devices on same LAN (like a hub). So we should add edges between any two devices that share a LAN. That's the most general.

So to build graph:
- Create a mapping from LAN to list of devices.
- Then for each LAN, for each pair of devices, add undirected edge.

Alternatively, we can think of each device with ports, but the port numbers are not needed for routing paths; only connections matter.

Then, identify all hosts: devices whose name starts with 'h' (like "h1", "h2"). Not switches.

Then for each pair of hosts (i, j) with i != j, find shortest path in the graph (using BFS since unweighted). The path should be a list of node names (device names). Return dictionary as described: per source host, a dict of destination host to path list (including both hosts).

Also, note: The output example shows "h1": {"h2": ["h1", "s1", "h2"]}. So path includes source and destination hosts, and intermediate switches. No LANs.

Edge case: if no path, we probably omit that pair? The problem statement says "routing paths" meaning only reachable. So we only include reachable hosts.

The function signature should be:

def compute_routing_paths(topo: Dict[str, Dict[int, str]], requirements = None) -> Dict[str, Dict[str, List[str]]]:

We need to import necessary packages: from collections import deque, defaultdict.

We'll implement BFS for each source host.

Also, note: The input topo is a dict of device to {port: LAN}. We'll assume it's correct.

Now, produce the code as a string inside JSON result.

The output JSON should have "status": "OK" and "result": "<function code as string>".

We need to escape the function code properly in JSON.

Let's write the code.